# 🚀 Spring Security + JPA + JWT + Redis
```bash
- Environments:
    1. Gradle 8.6
    2. JDK 21
- Requirements:
    1. MariaDB or MySql.
    2. Redis(can use Docker).
```
## 🛠️ Installation
Steps required to set up and install the project locally:
```bash
git clone https://github.com/quangchinh92/haha.git
cd JWTServerApp
gradle build
```

## 💻 Usage
Provide brief examples showing how to run or use the project:
```bash
1. Build jar: gradle build
2. Build Docker: docker build -t myapp:latest .
3. Start Docker: docker run -d -p 8080:8080 --name my-running-app myapp:latest
```